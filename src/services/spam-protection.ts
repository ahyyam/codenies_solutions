/**
 * Checks if the given text is likely to be spam.
 *
 * @param text The text to check.
 * @returns A promise that resolves to true if the text is spam, false otherwise.
 */
export async function isSpam(text: string): Promise<boolean> {
  // TODO: Implement this by calling an API.
  return false;
}
